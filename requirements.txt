# --- Núcleo FastAPI (stub mode) ---
# Rangos abiertos: pip elige la versión que tenga wheel precompilado
# para tu intérprete (Python 3.14 necesita pydantic >=2.10 en Windows).
fastapi>=0.116,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-multipart>=0.0.18

# --- IA real: clasificador de intención del asistente (CU-46) ---
# Modelo propio entrenado con TensorFlow (app/ml). CPU-only para el contenedor.
tensorflow-cpu==2.21.0

# --- Soporte futuro (descomenta cuando se entrenen mas modelos) ---
# scikit-learn==1.5.2
# pandas==2.2.3
# spacy==3.7.5
# openai-whisper==20240930

# --- AWS: Transcribe (STT) + Bedrock (LLM) para CU-39/CU-31 ---
# Solo se usa si AWS_AI_ENABLED=true; el import es perezoso, así que en local
# (sin AWS) el microservicio arranca igual aunque boto3 no aporte nada.
boto3>=1.35,<2.0

# --- Integración futura con Mongo ---
# pymongo==4.10.1
